# Docker Compose — root only needs web build vars.
# API secrets: apps/api/.env
# Web (pnpm): apps/web/.env.local
#
#   cp .env.example .env
#   cp apps/api/.env.example apps/api/.env
#   docker compose up --build

# --- Web ---
NEXT_PUBLIC_API_URL=http://localhost:4000
