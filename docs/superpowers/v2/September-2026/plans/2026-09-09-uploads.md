# Avatars + issue files Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Presigned avatar and issue-file uploads with MinIO in Compose.

**Architecture:** Prisma `attachments` + injectable `StorageClient`. Routes create pending rows and sign URLs. Browser PUTs to S3. Complete runs `HeadObject`. Web uses `*Api` + TanStack Query.

**Tech Stack:** Prisma, Express, `@aws-sdk/client-s3`, Next.js, Node `node:test`.

## Global Constraints

- Path aliases `@/`. Envelope unchanged. `NODE_ENV` only development/production.
- Shared `attachment.constant.ts`. Do not edit Circle mock-data.
- Do not git commit unless the human asks.

---

### Task 1: Schema, storage, API, web, Compose

- [x] Shared limits/kinds + `STORAGE_UNCONFIGURED`; Prisma `attachments` + `users.avatar_attachment_id`.
- [x] `StorageClient` (S3 + memory mock); intent/complete/list/delete routes; OpenAPI.
- [x] Profile picture + issue paperclip wired; MinIO in Compose; env documented.
- [x] Tests mock S3; mark step 20 done in docs.
