# Inbox Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** In-app inbox for comment, assignee, and status changes — current user, current org, polling only.

**Architecture:** `notifications` table (org + recipient scoped). Rows inserted in the same transaction as the comment or issue patch. List / mark-read / mark-all-read at `/orgs/:orgId/notifications`. Web: `inbox.service.ts` `*Api` + TanStack Query `refetchInterval` 5 mins; restore sidebar Inbox.

**Tech Stack:** Express, Prisma, PostgreSQL, Zod, TanStack Query, existing envelope + `requireAuth` + `requireOrgMember`

**Spec:** [docs/superpowers/specs/2026-08-28-inbox-design.md](../specs/2026-08-28-inbox-design.md)

## Global Constraints

- Tenancy: `requireAuth` → `requireOrgMember` → `req.org.id` and `req.user.id` — never authorize from URL alone
- Envelope: `{ success, message, data, error }` via `sendSuccess` / `sendError`
- Path aliases `@/` in API and web
- Constants in `*.constant.ts` (new domain `inbox`); mirror API and web keys/values for types and list limit
- Web HTTP only in `apps/web/services/*.service.ts` named `*Api`; UI uses TanStack Query
- No notification emails; no WebSockets / Redis; reviews stay hidden
- Do not commit unless the user explicitly asks
- Do not implement steps 13–17 in this plan

## File map

| Path | Responsibility |
|------|----------------|
| `apps/api/prisma/schema.prisma` | `Notification` + relations |
| `apps/api/prisma/migrations/20260828150000_add_notifications/migration.sql` | Table + indexes |
| `apps/api/src/constants/inbox.constant.ts` | Types + list limit |
| `apps/api/src/utils/inbox/notify.ts` | `shouldNotify` / `notifyIfRecipient` |
| `apps/api/src/utils/inbox/notify.test.ts` | Skip self / missing recipient |
| `apps/api/src/routes/inbox/notifications.ts` | List / mark-read / mark-all-read |
| `apps/api/src/routes/inbox/notifications.test.ts` | Tenant + emit + read tests |
| `apps/api/src/routes/issues/activity.ts` | Comment emit |
| `apps/api/src/routes/issues/issues.ts` | Status / assignee emit |
| `apps/api/prisma/seed.ts` | Welcome-issue comment notification |
| `apps/web/constants/inbox.constant.ts` | Types, poll, paths |
| `apps/web/constants/workspace.constant.ts` | Drop `INBOX` |
| `apps/web/services/inbox.service.ts` | HTTP wrappers |
| `apps/web/hooks/use-inbox.ts` | Query + mutations |
| `apps/web/lib/query-keys.ts` | `inbox(orgSlug)` |
| `apps/web/components/common/inbox/*` | API-backed list + preview |
| `apps/web/components/layout/sidebar/nav-inbox.tsx` | Restore Inbox + badge |
| `apps/web/store/sidebar-prefs-store.ts` | Default always + migrate |
| `apps/web/components/layout/sidebar/customize-sidebar-dialog.tsx` | Inbox in personal items |

---

### Task 1: Schema + emit helper

- [x] Spec written
- [ ] Constants + `shouldNotify` tests
- [ ] Schema + migration + `notifyIfRecipient`

### Task 2: API list/read + emit from comment/patch

- [ ] Failing tenant/emit/read tests
- [ ] Router mounted; comment + patch insert rows
- [ ] Tests pass

### Task 3: Seed + web

- [ ] Seed welcome-issue notification
- [ ] Services, hooks, inbox UI, nav restore, sidebar prefs
- [ ] Browser / API verify list, mark-read, isolation
