# Cycles Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Per-team timeboxed cycles — one active cycle per team, optional `issues.cycle_id`, Circle cycle pages show API list + dates.

**Architecture:** `cycles` table (org + team scoped). Nested router at `/orgs/:orgId/teams/:teamId/cycles`. Issue list filter + patch `cycleId`. Web: `cycles.service.ts` `*Api` + TanStack Query; restore team nav Cycles; hide burn-up.

**Tech Stack:** Express, Prisma, PostgreSQL, Zod, TanStack Query, existing envelope + `requireAuth` + `requireOrgMember`

**Spec:** [docs/superpowers/specs/2026-08-28-cycles-design.md](../specs/2026-08-28-cycles-design.md)

## Global Constraints

- Tenancy: `requireAuth` → `requireOrgMember` → `req.org.id` — never authorize from URL alone
- Envelope: `{ success, message, data, error }` via `sendSuccess` / `sendError`
- Path aliases `@/` in API and web
- Constants in `*.constant.ts` (new domain `cycle`); mirror API and web keys/values
- Web HTTP only in `apps/web/services/*.service.ts` named `*Api`; UI uses TanStack Query
- One active cycle per team (SQL + API)
- Burn-up / capacity APIs stay out of this slice
- Do not commit unless the user explicitly asks
- Do not implement steps 14–17 in this plan

## File map

| Path | Responsibility |
|------|----------------|
| `apps/api/prisma/schema.prisma` | `Cycle` + `Issue.cycleId` |
| `apps/api/prisma/migrations/20260828180000_add_cycles/migration.sql` | Tables + one-active unique index |
| `apps/api/src/constants/cycle.constant.ts` | Status + name max |
| `apps/api/src/constants/activity.constant.ts` | `IssueEventType.CYCLE` |
| `apps/api/src/utils/cycle/cycle.ts` | `isCycleStatus`, `parseCycleInstant`, `assertCycleOnTeam` |
| `apps/api/src/routes/cycles/cycles.schema.ts` | Create/patch Zod |
| `apps/api/src/routes/cycles/cycles.ts` | List/create/update |
| `apps/api/src/routes/cycles/cycles.tenant.test.ts` | Tenant + one-active + CRUD |
| `apps/api/src/routes/issues/issues.ts` | `cycleId` on list/create/patch |
| `apps/api/src/routes/issues/issues.schema.ts` | `cycleId` fields |
| `apps/api/src/routes/issues/cycles.test.ts` | Filter + patch + event |
| `apps/api/src/openapi/paths/cycles.ts` | Register paths |
| `apps/api/prisma/seed.ts` | Seed cycles + attach first issue |
| `apps/web/constants/cycle.constant.ts` | Mirrored consts + date range helper |
| `apps/web/constants/activity.constant.ts` | `CYCLE` |
| `apps/web/services/cycles.service.ts` | HTTP wrappers |
| `apps/web/hooks/use-cycles.ts` | Query + mutations |
| `apps/web/lib/query-keys.ts` | `cycles(orgSlug, teamId)` |
| `apps/web/lib/mappers.ts` | Map `issue.cycleId` |
| `apps/web/components/layout/sidebar/nav-teams.tsx` | Restore Cycles link |
| `apps/web/components/common/cycles/*` | API list + hide burn-up |
| `apps/web/components/common/issues/cycle-issues.tsx` | API cycle + `listIssuesApi` |

---

### Task 1: Schema + constants

- [ ] Spec written
- [ ] Constants + date helper
- [ ] Schema + migration

### Task 2: Cycles CRUD API

- [ ] Failing tenant / one-active tests
- [ ] Router mounted on teams
- [ ] Tests pass

### Task 3: Issue cycleId + events

- [ ] Failing filter / patch / event tests
- [ ] List filter, create/patch `cycleId`, `cycle` event
- [ ] Tests pass

### Task 4: OpenAPI + seed + web

- [ ] OpenAPI paths + seed
- [ ] Services, hooks, nav, cycle pages, hide burn-up
- [ ] Browser verify team nav + cycles list + dates
