'use client';

/**
 * Legacy location — the old filter popover was replaced by the
 * Linear-style filter bar (bazza/ui data-table-filter).
 * Kept as a re-export so historical imports keep working.
 */
export { IssueFilterBar as Filter } from '@/components/common/issues/issue-filter-bar';
